<h1><%= title %></h1>
<p>Lista de usuários:</p>

<ul>
  <% users.forEach(user => { %>
    <li><strong><%= user.nome %></strong> - <%= user.idade %> anos</li>
  <% }) %>
</ul>

<a href="/">Voltar para a página inicial</a>
